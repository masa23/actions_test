name: onpush 

on: [ push ]

env:
  HOGE: "hoge"

jobs:
  ontest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ls
        id: check_file
        run: |
          echo $HOGE
          HOGE="fuga"
  hoge:
    runs-on: ubuntu-latest
    needs: ontest
    steps:
      - name: echo file_exists
        run: |
          cat $HOGE
