name: onpush 

on: [push]

jobs:
  ontest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello, World!
        run: |
          echo "Hello, World!"
          ls -la ./.github/workflows/hoge.yaml
